# Fonts

The app now uses the device system font by default, especially on installed iPhone PWAs.

Users can upload a custom global font from Settings. Uploaded fonts are stored locally in IndexedDB instead of being bundled in this folder.

Use only font files you have the right to use.
